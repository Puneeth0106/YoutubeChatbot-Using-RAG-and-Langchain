// Background service worker (currently unused) included to satisfy MV3 manifest.
// We keep it minimal for now but it can be used for future messaging or alarms.

chrome.runtime.onInstalled.addListener(() => {
  // no-op
});
